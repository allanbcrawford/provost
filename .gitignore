node_modules/
.turbo/
.next/
.convex/
# convex/_generated is tracked so Vercel can build without running codegen
# (Convex's codegen in CI requires CONVEX_DEPLOY_KEY which we don't set for
# every Vercel build; committing generated types is an officially-supported
# pattern for monorepos)
dist/
build/
.env
.env.*
.env.local
!.env.example
.DS_Store
*.log
coverage/
storybook-static/
*.tsbuildinfo
.vercel
.claude/settings.local.json

# Stale Convex codegen artifact — root convex/ is canonical
apps/web/convex/
