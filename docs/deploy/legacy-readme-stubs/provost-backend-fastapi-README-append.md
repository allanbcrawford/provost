> **ARCHIVED — This repository is no longer actively maintained.**
>
> Active development for the Provost wealth-governance platform has moved to
> [`provost/`](https://github.com/sanctifai-wealth/provost).
>
> This repository is preserved in read-only mode for historical reference.
> The final state of the code is tagged as [`v-legacy-final`](../../releases/tag/v-legacy-final).
>
> The FastAPI backend has been replaced by [Convex](https://convex.dev) as the backend
> platform. API endpoints that previously lived here are now implemented as Convex
> functions in the `convex/` directory of the new monorepo.
>
> Do not open pull requests or file issues here. Use the new repository instead.

---
